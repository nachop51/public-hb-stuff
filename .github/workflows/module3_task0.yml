name: Task 1

on:
  schedule:
    - cron: '0 0 * * *'

  push:
    branches:
      - main

jobs:
  run-test:
    name: Tests the help command of the Makefile
    runs-on: ubuntu
    steps:
      - name: Checkout
        uses: actions/checkout@v3
      - name: Run make help
        run: make help
